# carol-f12

From: fog
Started: 2026-09-18

## What I am trying

carol-f11 again with the bottom margin widened so the strapline stops colliding with the x labels.
Two characters of difference in the script; same numbers; a different figure file with the same
basename `headline.png`. Deliberately kept as a separate run, because "which run produced this
figure" is only an interesting question when two runs produce files with the same name.
